<h1>Movies</h1>

<a href="/movies/new">Add Movie</a>
<a href="/logout">Logout</a>

<ul>
    <% movies.forEach(movie => { %>
        <li>
            <a href="/movies/<%= movie._id %>">
                <%= movie.name %>
            </a>
        </li>
        <% }) %>
</ul>